#!/usr/bin/python3
#Write a script to replace all occurrences of a character, c, with another character, d.

S = input("Enter string: ")
c = input("Enter character to replace: ")
n = input("Enter character to replace with: ")
new = S.replace(c,n)
print(new)
